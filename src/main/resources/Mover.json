{
    "id": "${id}",
    "firstName": "${firstName}",
    "lastName": "${lastName}",
    "username": "${username}",
    "password": "${password}",
    "memberSince": "${memberSince}",
    "group": {
        "id": "${id}",
        "name": "${name}",
        "city": "${city}",
        "state": "${state}"
    },
    "years": "${years}",
    "months": "${months}"
}
