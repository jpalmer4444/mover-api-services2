{
    "id": "${id}",
    "firstName": "${firstName}",
    "lastName": "${lastName}",
    "username": "${username}",
    "memberSince": "${memberSince}",
    "group": {
        "id": "${id}",
        "name": "${name}",
        "city": "${city}",
        "state": "${state}"
    },
    "years": "${years}",
    "months": "${months}"
}
